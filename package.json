{
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=rage-score -- --host",
    "build:web": "npm run build --workspace=rage-score"
  }
}
